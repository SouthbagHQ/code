{
	"name": "@southbag/code-website",
	"version": "0.83.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bunx astro build && bunx wrangler dev --env-file ../../.env --var DEV_AUTH:true",
		"dev:site": "bunx astro dev",
		"build": "bunx astro build",
		"preview": "bunx astro build && bunx wrangler dev --env-file ../../.env --var DEV_AUTH:true",
		"deploy": "bunx astro build && bunx wrangler deploy",
		"clean": "shx rm -rf dist .astro"
	},
	"dependencies": {
		"jose": "6.2.8"
	},
	"devDependencies": {
		"astro": "6.3.1",
		"wrangler": "4.120.0"
	}
}
